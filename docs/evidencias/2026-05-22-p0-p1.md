# Evidências de Execução - P0 e P1

Data: 2026-05-22

## Escopo validado

- P0: estabilização técnica, Docker, build, lint, SMTP seguro e README.
- P1: fluxo mínimo de coleta, e-mail obrigatório, regex de app, campos adicionais, roteamento PJ e handoff.

## Testes executados

| Teste | Resultado |
|---|---|
| `npm ci` | Passou |
| `npm run lint` | Passou |
| `npm run build` | Passou |
| `docker compose build` | Passou |
| `docker compose up -d` | Passou |
| `GET /api/health` | `{"status":"ok","db":"up","llm":"fallback"}` |
| `GET /server.js` no frontend | `404 Not Found` |
| Verificação de `.env`/`secrets` na imagem backend | Nenhum arquivo encontrado em `/app` |
| Smoke test `POST /api/chat` | Passou |
| Data `12/05/1990` | Não disparou FAQ de app e não marcou uso por app |
| Fluxo PF-1 completo | Roteado para `PF-1` |
| Mensagem após roteamento | Retornou status `ROUTED` sem reprocessar coleta |
| Fluxo PJ complexo | Roteado para `PJ-2` |
| Sinistro/urgência | Retornou `HUMAN_HANDOFF` |

## Observações

- O ambiente testado estava com Gemini em modo fallback porque o secret local estava vazio.
- O SMTP permaneceu desabilitado por variáveis vazias, conforme esperado em desenvolvimento.
- Os containers permaneceram saudáveis após os testes:
  - `seguro-auto-postgres`: healthy
  - `seguro-auto-backend`: healthy
  - `seguro-auto-frontend`: running
